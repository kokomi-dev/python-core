"""
import math để sử dụng các hàm toán học trong python
help(math) => để xem thêm các hàm tính toán trong python
"""
from math import *
# hàm căn bậc hai
s = sqrt(66)
# hàm làm tròn kết quả khi căn bậc hai
isqrt()

# hàm tính lũy thùy
pow(2,10)

# hàm làm tròn lên ( làm tròn lên số nguyên gần nhất lớn hơn tham số được truyền vào)
ceil() 

# hàm làm tròn xuống
floor()

# hàm  dai thừa
factorial(3)

# hàm lấy giá trị tuyệt đối
fabs()

# tìm giá trị nhỏ nhất, lớn nhất
min(), max()

# hàm tính tổng
sum()